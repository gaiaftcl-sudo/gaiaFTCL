---
description: GaiaFTCL Fusion limb — plug-and-play plant architecture (many fusion concepts), PCS backends, Metal settled, Mac ports, closed mesh, DMG (FoT8D workspace)
alwaysApply: true
---

# GaiaFTCL Fusion limb — plug-and-play plant architecture

Workspace may be **`FoT8D`** (monorepo root). Code under **`cells/fusion/`**.

## Multi-concept fusion (not tokamak-only)

Public taxonomy (magnetic confinement): **tokamak**, **stellarator**, **field-reversed configuration (FRC)**, **spheromak**, **magnetic mirror** (and variants). **Inertial confinement** (e.g. laser-driven laboratory implosion paths) is a **different plant class** with its own control/diagnostics surface. The **same GaiaFTCL operator app and mooring contract** applies across **which** facility concept and **which** vendor PCS stack is behind the bridge — **plug-and-play**, not a per-device forked UI.

## Plug-and-play PCS / plant I/O (same app)

Industry supplies **PCS**, central control, timing, and machine protection across fusion programs (e.g. open-control lineages such as **EPICS / CODAC-style** stacks in large facilities; integrators advertise fusion I&C and plasma control across **multiple experiments**). **Plant builders and control vendors** attach test bench, simulator, or live machine **without changing the Founder-facing app**: mooring, Fusion S⁴ console, wallet-gate path, DMG story stay **one surface**. Binding: **`cells/fusion/deploy/fusion_cell/config.json`**, **`cells/fusion/deploy/fusion_mesh/fusion_projection.json`**, **`bridges.*.invoke`**, site OPC/DAQ/SSH. **Not “mock”** — **interchangeable I/O backends**.

## Metal / cell-side shader path (settled)

**FusionControl** precompiled Metal (`default.metallib`). Receipt regression only if `cells/fusion/evidence/fusion_control/long_run_signals.jsonl` (legacy: `overnight_signals.jsonl`) diverges (`gpu_fused_multicycle`, `metallib`, `gpu_wall_us`).

## S4 projection

Catalog, cell config, Fusion UI under **`cells/fusion/deploy/fusion_mesh/`**, **`cells/fusion/deploy/fusion_cell/`**. Site endpoints site-defined. **C4 wins.**

**Bibliography / macro framing:** **`cells/fusion/evidence/fusion_control/FUSION_UNIVERSAL_PLANT_CONTROL_ASSESSMENT.md`**

## Mac ports

**`cells/fusion/deploy/mac_cell_mount/MAC_FUSION_MESH_CELL_PORTS.md`**, **`cells/fusion/services/gaiaos_ui_web/app/lib/macFusionCellPorts.ts`**. **C⁴ 8803 / 4222 / 8900**; **S⁴ 8910 / 14222**.

## Mesh closed · DMG on nine cells

**`cells/fusion/scripts/deploy_dmg_to_mesh.sh`**, build **`cells/fusion/scripts/build_gaiaftcl_facade_dmg.sh`**.

Fusion-cell-only workspace copy: **`cells/fusion/.cursor/rules/mac-fusion-tokamak-mesh-packaging.mdc`**.

Norwich — **S⁴ serves C⁴.**
