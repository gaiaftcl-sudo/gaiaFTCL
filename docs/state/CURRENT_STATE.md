# Substrate Projection

## Green
- **OWL-NUTRITION** phased plan delivered on `main`: gap table + Communion decision (b), invariant family tree, JSON schemas (incl. composite C4 fixture), MacHealth Nutrition Swift sources (target membership manual), `wasm_constitutional` nutrition exports + SHA-256 audit digest tests, IQ/OQ/PQ + CAB + audit docs, wiki Qualification Catalog + Home link, Phase 9 dry-run receipt.
- Substrate pulled (13 commits, Rust + Swift + evidence/ dirs).
- Wiki stubbed.
- Git hooks, Cursor rules, and docs skeleton installed.
- `gaitfc[l]` typo fully corrected across Swift files.
- Git commit signing configured locally for `gaiaftcl-sudo` using a dedicated sovereign SSH key.
- `gaiaftcl-sudo` SSH key registered in GitHub for authentication.
- S⁴C⁴ hash migration completed for `cell_identity.json`. `identityHashProvider` now deterministically fuses S4 topological anchor and C4 cryptographic anchor.
- Mac Cell CI workflow implemented for macOS-only, aarch64-apple-darwin Rust FFI architecture.

## BLOCKED
- None.

## Next Receipt
- None.
