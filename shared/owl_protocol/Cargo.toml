[package]
name        = "owl-protocol"
version     = "0.1.0"
edition     = "2021"
description = "Owl cryptographic identity types — zero-PII, shared between all Gaia cells"

[dependencies]
sha2 = "0.10"
hex  = "0.4"
