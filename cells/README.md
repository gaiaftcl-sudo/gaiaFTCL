# Mac cells

This directory holds **GaiaFTCL Mac cell** product trees.

| Cell | Path | Role |
|------|------|------|
| **Fusion** | [`fusion/`](fusion/) | GaiaFusion / plasma / Mac Fusion stack (former `cells/fusion/` tree). |
| **Health** | [`health/`](health/) | Mac Health / Biologit stack (former top-level `cells/health/`). |

Future cells follow the same pattern: `cells/<name>/`.
