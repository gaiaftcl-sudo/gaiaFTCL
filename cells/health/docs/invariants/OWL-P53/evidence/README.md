# Evidence — OWL-P53-INV1

Place qualification artifacts under the subfolders:

- **`iq/`** — installation receipts, environment captures, checksum logs.  
- **`oq/`** — synthetic run outputs, drift scenarios, signed logs.  
- **`pq/`** — PQ-v1 analysis plans and results; **no** PHI in PQ-v1 without DUA.  
- **`references/`** — bibliographic stubs and PDF pointers (no copyright text).

Naming: use ISO-8601 prefixes and invariant ID in filenames where practical.
