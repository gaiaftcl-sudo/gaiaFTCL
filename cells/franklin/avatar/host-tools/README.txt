Host-built CLIs shipped so sprout Gate E can sign bundles on a fresh clone without a full Rust workspace checkout.

darwin-arm64/sign_bundle — copied from cargo build (release) of tools/sign_bundle for this repo.
Intel Mac / Linux: build target/release/sign_bundle from the full avatar Cargo workspace, or add host-tools/<platform>/sign_bundle the same way.
