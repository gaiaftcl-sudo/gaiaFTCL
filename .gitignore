# OS files
.DS_Store
**/.DS_Store
*.swp
*.swo
*~

# Cursor / IDE / local editor — do not commit (operator machine only)
.cursor/
**/.cursor/
.cursorrules
**/.cursorrules
.cursorignore
**/.cursorignore
.vscode/
**/.vscode/
.idea/
**/.idea/
*.code-workspace
cells/fusion/gaiaos_dot_cursor/

# Build artifacts
target/
/target
**/target/
*.so
*.dylib
*.a
swift_testrobit/libgaia_metal_renderer.a
swift_testrobit/.build/
.build/

# Dependencies
node_modules/

# Data directories
data/
backups/
logs/

# Secrets
.env
*.pem
*.key

# Large model files
*.gguf
*.bin
*.safetensors
models/*/model.*

# AGI binaries (local builds)
.agi_binaries/

# Temporary files
tmp/
/tmp/
temp/
*.tmp

# Python
__pycache__/
*.pyc
.venv/
venv/

# GaiaHealth — sovereign wallet (zero-PII but private entropy must not be committed)
.gaiahealth/
cells/health/evidence/
cells/health/swift_testrobit/*.a
cells/health/wasm_constitutional/pkg/

# GAIAFTCL — existing wallet pattern
.gaiaftcl/
GAIAFTCL/evidence/

# Xcode
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/

# SPM
.swiftpm/
.build/
# NOTE: Package.resolved IS committed — it's your lockfile

# Rust
target/
**/*.rs.bk

# Secrets & local state (repo root + operator home)
.env.local
~/.gaiaftcl/
*.keychain

# OWL-NUTRITION local IQ/OQ/PQ run receipt (bash scripts/owl_nutrition_iqoqpq_validate.sh)
cells/health/docs/invariants/OWL-NUTRITION/evidence/owl_nutrition_iqoqpq_receipt.json

# Runtime evidence (generated each run)
evidence/*.actual.json
evidence/full_cycle_receipt.json
evidence/oq_receipt.json
evidence/iq_receipt.json
evidence/testrobit_receipt.json
evidence/vqbit_test_receipt.json

# Finder duplicates (already deleted, ensure they stay gone)
*\ 2.*

# Local-only trees / side clones — do not commit (GAMP hygiene)
.claude/
BenOS/
GAIAFTCL/
cells/fusion/Internal/
cells/fusion/archive/
cells/fusion/cursor_watchdog_*.log
gaia-ftcl-dist/

# Git LFS hooksPath — local only; never commit (not project source)
.githooks/

gaiaFTCL.wiki/
